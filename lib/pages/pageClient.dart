import 'package:flutter/material.dart';


class PageClient  extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("PageClient"),
    );
  }
}